services:
  ps_dictionary.commands:
    class: Drupal\ps_dictionary\Commands\DictionaryCommands
    arguments:
      - '@ps_dictionary.manager'
      - '@entity_type.manager'
    tags:
      - { name: drush.command }
