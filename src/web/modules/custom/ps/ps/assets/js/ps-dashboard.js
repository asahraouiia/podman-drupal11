/**
 * @file
 * Dashboard JavaScript for PropertySearch.
 */

(function (Drupal) {
  'use strict';

  Drupal.behaviors.psDashboard = {
    attach: function (context, settings) {
      // Dashboard behavior placeholder.
    }
  };

})(Drupal);
